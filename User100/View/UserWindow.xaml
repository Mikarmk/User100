<Window x:Class="User100.View.UserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:User100.View"
        mc:Ignorable="d"
        Title="Окно пользователя" Height="500" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TextBlokUser"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ItemsTovar">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="200"
                                Margin="10"
                                Padding="10"
                                BorderBrush="Gray"
                                BorderThickness="1"
                                CornerRadius="8">
                            <StackPanel>
                                <Image Source="{Binding Foto}" Height="140" Stretch="UniformToFill"/>
                                <TextBlock Text="{Binding Articul}" Margin="0,8,0,0" FontWeight="Bold"/>
                                <TextBlock Text="{Binding TovarName}" Margin="0,4,0,0" FontSize="15"/>
                                <TextBlock Text="{Binding Cena, StringFormat=Цена: {0}}" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding Kolichestvo, StringFormat=Количество: {0}}" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
