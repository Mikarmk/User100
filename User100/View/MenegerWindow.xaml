<Window x:Class="User100.View.MenegerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:User100.View"
        mc:Ignorable="d"
        Title="Окно менеджера" Height="500" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TextBlockUser"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <TextBox x:Name="SearchBox"
                 Grid.Row="1"
                 Width="220"
                 Height="28"
                 HorizontalAlignment="Left"
                 Margin="0,0,0,10"
                 TextChanged="SearchBox_TextChanged"/>

        <DataGrid x:Name="DG_Tovar"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Артикул" Binding="{Binding Articul}" Width="100"/>
                <DataGridTextColumn Header="Название" Binding="{Binding TovarName}" Width="150"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Cena}" Width="90"/>
                <DataGridTextColumn Header="Скидка" Binding="{Binding Skidka}" Width="90"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Kolichestvo}" Width="110"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Opisanye}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
